# ── Vercel Python runtime dependencies ──────────────────────────────────
# This file is consumed when the backend/ directory is deployed as a
# Vercel project. Keep it in sync with the root requirements.txt.

serverless-wsgi>=3.0
Django>=6.0,<7.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.4
django-cors-headers>=4.6
pandas>=2.2
numpy>=2.2
openpyxl>=3.1
xlsxwriter>=3.2
reportlab>=4.3
psycopg2-binary>=2.9
whitenoise>=6.7
django-storages>=1.14
boto3>=1.34
